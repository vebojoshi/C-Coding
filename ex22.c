#include<stdio.h>

int main()
{
	
	char ch;
	if(ch=printf(""))
	 printf("It matters\n");	
	else
	 printf("It doesn't	matters\n");
	 return 0;
}

//o/p-> It doesn't matters  
//concept  ->   printf() function returns int value hence nothing in printf retuens 0 to if statement 
//              so else will execute.
